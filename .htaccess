RewriteEngine On
RewriteBase /

# Serve ficheiros e diretórios reais normalmente
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Enviar qualquer outra rota para a SPA (index.html)
RewriteRule . /index.html [L]


